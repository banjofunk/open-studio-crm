import React from 'react'
import { Classes } from '@blueprintjs/core'
import classnames from 'classnames'

export default () =>
  <h2 className={classnames(Classes.TEXT_MUTED, 'sidebar-header')}>Schedule</h2>
